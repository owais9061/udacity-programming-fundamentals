def rest_of_string(o):
    return o[:6]

print (rest_of_string('SAMRAH'));

#What happen here is that when we assign a parameter (o) to the function
#and give a command to return only values from 0 to 3
#then wehen we print function with ('string'), what it does, is automatically
#assign the string to the parameter we set above