name="BSOC"
print(name[3]);
#Now
print(name[0:2]);
#If we have same ending and starting point then
print(name[1:1]);
#also we can do this
print(name[:3]);
#The character after or before the colon (:) is not included in the string.