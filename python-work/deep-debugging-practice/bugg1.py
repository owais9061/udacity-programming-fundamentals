# A small change will fix the crashing bug in printInches.
# solve using a google search of the error you recive

def printExample(a, b):
   print(str(a + b))
    
def printInches(n):
   print(str(n) + " inches")           #here is here to be resolved

# Don't change the function calls on lines 10 - 12.
printExample(17, 23)
printExample('long', 'word')
printInches(42)